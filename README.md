## To create a server

```
npm init
```

## To use express, install it

```
npm install express
```

Now require it:

```
const express = require("express");
```

Create a server with express:

```
const app = express()
```
